import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const CamerasScreen = () => {
  return (
    <View>
      <Text>CamerasScreen</Text>
    </View>
  );
};

export default CamerasScreen;

const styles = StyleSheet.create({});
